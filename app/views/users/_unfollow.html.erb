<%= form_for(current_user.following_a_user.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |form| %>
  <%= form.submit "Unfollow", class: "btn" %>
<% end %>